import { StyleSheet } from "react-native";

export const Shadows = StyleSheet.create({
  DashboardProductShadow: {
    shadowColor: "blue",
    shadowOffset: {
      width: 0,
      height: 3,
    },
    shadowOpacity: 0.1,
    shadowRadius: 3,
    elevation: 1,
  },
});
